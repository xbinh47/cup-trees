version: '3.8'

services:
  bracket-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bracket-tools
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
